# 브라우저가 호출할 API 기본 주소.
# 비워 두면 같은 오리진의 /api/v1 을 쓰고, next.config.mjs 의 rewrite 가 백엔드로 넘깁니다.
NEXT_PUBLIC_API_BASE_URL=

# rewrite 가 바라볼 백엔드 오리진. 미설정 시 배포 백엔드를 가리킵니다.
# 로컬 백엔드로 붙일 때만 .env.local 에 아래 값을 둡니다.
# BACKEND_ORIGIN=http://localhost:8080
