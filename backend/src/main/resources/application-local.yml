spring:
  datasource:
    url: jdbc:mysql://localhost:3306/slotkey?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: root

app:
  auth:
    cookie:
      secure: false
      same-site: Lax